# 概要
このリポジトリは [Pythonでダッシュボードを作成！Dash＋Plotlyを解説！〜初心者向け〜 業務効率化](https://www.youtube.com/watch?v=L6S0ivlgq7g) を試したときに作ったものです。

## 構成
- uv で必要なライブラリをインストールしています。
    - `uv add dash`
    - `uv add pandas`
- リポジトリを別途取得後は `uv sync` で必要なライブラリが取得できます。
- 実行時は `uv run <実行するファイル.py>` で起動できます。
